// writers.ts — kept minimal; dynamic writers are built by session.ts
// WRITER_IDS / WRITERS are only used in legacy CSS references; session.ts handles identity

export const WRITER_IDS = ["alice", "bob"] as const;
